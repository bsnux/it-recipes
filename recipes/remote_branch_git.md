Creating a new remote branch in a git repository
-------------------------------------------------

First we need to create a branch locally and then pushing to our repo:

    $ git branch <branch_name>
    $ git push origin <branch_name>